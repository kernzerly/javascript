function sqrt(num) {
  if(!isNaN(convertedNum))
  return alert (convertedNum) * num

} else {
  alert (NaMN);
  let newAnswer = prompt ("Give me something else");
  sqrt(newAnswer);

}
function alertString(str)
if(str.charAt(str.length - 1))
str += '.';
}
str = str.charAt(0).toUpper(
  alert(str);
)
// let myStr = prompt('Hey,

stringToFlip("").reverse().


function definePal(string)
let reverse =
 string.split('').reversed().
}
